import { text } from '@saerskriven/i18n';

/** What a control is called, drawn or spoken, across the panel and the canvas. */
export const fieldMessages = {
  title: text(),
  description: text(),
  category: text(),
  severity: text(),
  status: text(),
  'diagram-title': text(),
  'note-text': text(),
  'name-of': text({ element: 'text' }),
  'record-name': text({ kind: 'text', number: 'number' }),
  'record-title-field': text({ name: 'text' }),
  'record-prose-field': text({ name: 'text' }),
  'record-status-field': text({ name: 'text' }),
  'add-record': text({ kind: 'text' }),
  'existing-record': text({ kind: 'text' }),
  'link-existing-record': text({ kind: 'text' }),
  'choose-existing-first': text({ kind: 'text' }),
  'discard-record': text({ kind: 'text', number: 'number' }),
  'unlink-record': text({ kind: 'text', number: 'number' }),
  'existing-threat': text(),
  'attach-existing-threat': text(),
  'choose-existing-threat-first': text(),
  'existing-element': text(),
  'attach-existing-element': text(),
  'choose-existing-element-first': text(),
  'detach-element': text({ element: 'text' }),
  'provides-authentication': text(),
  'handles-card-payments': text(),
  'handles-goods-or-services': text(),
  'web-application': text(),
  'privilege-level': text(),
  'log-store': text(),
  'encrypted-storage': text(),
  'signed-storage': text(),
  'stores-credentials': text(),
  'stores-inventory': text(),
  'encrypted-flow': text(),
  'public-network': text(),
  protocol: text(),
  'crossed-trust-boundaries': text(),
  'crossed-trust-boundaries-lower': text(),
  'contained-elements': text(),
  'contained-elements-lower': text(),
  'crossing-flows': text(),
  'crossing-flows-lower': text(),
  'recording-of': text({ label: 'text' }),
  'relationship-item': text({ label: 'text', number: 'number' }),
  'remove-relationship': text({ label: 'text', number: 'number' }),
  'add-to-relationship': text({ label: 'text' }),
} as const;
